{
  "name": "@anthos/feature-flags",
  "version": "0.0.0",
  "private": true,
  "description": "Vendor-agnostic feature-flag SDK wrapper (ARC-22). Hides the flag provider behind isEnabled / getVariant; Demo-Mode provider is Vercel Edge Config.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run --root ../.. packages/feature-flags/test"
  },
  "dependencies": {
    "@anthos/auth": "workspace:*",
    "@vercel/edge-config": "^1.4.3"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
