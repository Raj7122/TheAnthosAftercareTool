{
  "name": "@anthos/integrations",
  "version": "0.0.0",
  "private": true,
  "description": "External system clients: Salesforce, Mogli, MS Graph (Outlook), email via Salesforce Flow.",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run --root ../.. packages/integrations/test"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
