{
  "name": "@anthos/logging",
  "version": "0.0.0",
  "private": true,
  "description": "P1A-06: structured JSON logging (MON-LOG-2) + X-Trace-Id propagation (API §8.5). CloudWatch-shaped records; PII firewall on log fields (SEC-AUDIT-4 by extension).",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthos/audit": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
